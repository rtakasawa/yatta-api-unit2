<div class="container">
  <div class="card">
    <div class="card-header">
      会員情報
    </div>
    <div class="card-body">
      <p class="card-text">名前 : <%= @user.name %></p>
      <p class="card-text">メールアドレス : <%= @user.email %></p>
      <%= link_to "会員情報：編集・退会", edit_user_registration_path(@user), class: "btn btn-primary" %>
    </div>
  </div>
</div>